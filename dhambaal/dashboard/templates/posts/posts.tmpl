{% extends "layout/base.html" %}
 {% block content %}
{%from "macros.html" import section_heading %}
{{section_heading(title="Posts", link="admin.create_post", btn_text="Create Post")}}
<table class="table box">
  <thead>
    <tr>
      <th>Id</th>
      <th>Title</th>
      <th>Description</th>
      <th>Category</th>
      <th>Date</th>
     <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    {%for post in posts%} 
    <tr>
    <td>{{post.id}}</td>
      <td><a href="#" >{{post.title}}
      </td>
      <td>{{post.description}}</td>
      <td>{{post.category}}</td>
      <td>{{post.date}}</td>
      <td><a>Delete</a> | <a>Edit</a></td>
    </tr>
    {%endfor%}
    </tbody>
    </table>
    {% endblock %}